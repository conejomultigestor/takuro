/* Takuro v2 — configuración.
   RELAY: URL del WebSocket del relay.
   Si se deja vacío, la app queda en modo simulación (bots) y sirve igual. */
window.TK_CONFIG = {
  // Ejemplo en deploy: wss://takuro-relay.onrender.com/ws
  relay: "",
};